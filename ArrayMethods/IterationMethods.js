//-------------------Array Iteration methods

let array = ['a','b','c']

//---------------ENTRIES

let entry = array.entries();
for(let entry of a){
    console.log(entry); // output: [ 0, 'a' ] [ 1, 'b' ] [ 2, 'c' ]
}

//-------------KEYS

let key = array.keys();
for(let entry of a){
    console.log(entry);// output: 0 1 2
}

//-------------VALUES

let value = array.values();
for(let entry of a){
    console.log(entry);// output: a b c
}
